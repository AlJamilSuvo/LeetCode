#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    int maximumGood(vector<vector<int>> &statements)
    {
        int maxGoodPersonCount = 0;
        int peopleCount = statements.size();
        int key = peopleCount - 1;
        set<int> good;
        set<int> bad;
        

        return maxGoodPersonCount;
    }
};