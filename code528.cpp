#include <bits/stdc++.h>
using namespace std;

class Solution
{
    vector<int> sum;

public:
    Solution(vector<int> &w)
    {
        sum.push_back(w[0]);
        for (int i = 1; i < w.size(); i++)
        {
            
        }
    }

    int pickIndex()
    {
    }
};