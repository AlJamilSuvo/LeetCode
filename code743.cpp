#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    bool areSentencesSimilar(vector<string> &sentence1, vector<string> &sentence2, vector<vector<string>> &similarPairs)
    {
        map<pair<string, string>, bool> similarPairMap;
        for (auto similarPair : similarPairs)
        {
          
        }
    }
};